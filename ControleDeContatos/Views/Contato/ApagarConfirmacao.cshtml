@model ContatoModel
@{
    ViewData["Title"] = "Confirmação de apagar o contatos";
}

<div class="text-center">

    <h3>Realmente deseja apagar o contato (@Model.Nòme)?</h3>
    <br />
</div>

<div class="text-center">
<a class="btn btn-primary" asp-controller="Contato" asp-action="Index" role="button">Cancelar exclusão</a>
<a class="btn btn-danger" asp-controller="Contato"asp-route-id="@Model.Id" asp-action="Apagar" role="button">Confirmar exclusão</a>
